[tools]
# bun runs the CLI (about twice as fast as node, mostly on startup); node is
# still needed for the Raycast extension's build, and is the reference runtime
# for the test suite.
bun = "1.4.2"
node = "26.7.0"
pnpm = "10.33.0"

[env]
_.path = ["{{config_root}}/node_modules/.bin"]
